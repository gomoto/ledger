{
  "name": "ledger",
  "version": "0.0.0",
  "dependencies": {
    "@types/express": "4.16.0",
    "@types/graphql": "14.0.4",
    "apollo-server-express": "2.3.1",
    "express": "4.16.4",
    "graphql": "14.0.2"
  },
  "devDependencies": {
    "@types/node-fetch": "2.1.4",
    "aws-sdk": "2.382.0",
    "node-fetch": "2.3.0",
    "ts-node": "7.0.1",
    "typescript": "3.2.2"
  },
  "scripts": {
    "prebuild": "rm -rf build",
    "build": "tsc -p tsconfig.build.json --outDir build; npm run copy",
    "copy": "cp src2/schema.graphql build/schema.graphql",
    "start": "node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "10.x.x",
    "npm": "5.x"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gomoto/ledger.git"
  },
  "keywords": [
    "aws",
    "appsync",
    "graphql"
  ]
}
